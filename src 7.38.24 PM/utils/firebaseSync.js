// Placeholder for optional Firebase sync hooks.
// This file is NOT used by default and contains no keys.
// To enable later:
// 1) Install Firebase SDK: npm i firebase
// 2) Fill config and initialize app
// 3) Wire functions into storage save/load flows.

/*
// import { initializeApp } from 'firebase/app'
// import { getFirestore, doc, getDoc, setDoc } from 'firebase/firestore'

// const firebaseConfig = {
//   apiKey: 'YOUR_API_KEY',
//   authDomain: 'YOUR_AUTH_DOMAIN',
//   projectId: 'YOUR_PROJECT_ID',
//   storageBucket: 'YOUR_BUCKET',
//   messagingSenderId: 'YOUR_SENDER_ID',
//   appId: 'YOUR_APP_ID',
// }

// const app = initializeApp(firebaseConfig)
// const db = getFirestore(app)

export async function pullState(userId) {
  // const ref = doc(db, 'planorama', userId)
  // const snap = await getDoc(ref)
  // return snap.exists() ? snap.data() : null
  return null
}

export async function pushState(userId, state) {
  // const ref = doc(db, 'planorama', userId)
  // await setDoc(ref, state, { merge: true })
}
*/
